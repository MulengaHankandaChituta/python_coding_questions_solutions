# Write a python program
# to find the highest common denominator

import math

def gcd(a, b):
    return math.gcd(a, b)

result = gcd(48, 18)
print(f"GCD of 48 and 18 is {result}")

# python's math.gcd() function calculates
# the greatest common divisor